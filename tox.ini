[tox]
envlist = py27,py35
[testenv]

deps=
    nose
    coverage

commands=
    nosetests --logging-format='%(asctime)s [%(name)s] %(levelname)-6s %(message)s' \
                --with-coverage \
                --cover-package=tornado_xmlrpc []
